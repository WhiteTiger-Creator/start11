Act as the storage engineer brought in after a compaction was interrupted mid-run. Nine segment files under `/app/data/pending` reached disk but were never linked into `/app/data/manifest.json`, one of them torn, and the compactor died with only the cold level merged, so `/app/data/compacted_base.jsonl` and `/app/data/manifest_repaired.json` both stop well short of the tree that is actually on disk. Reconcile them first. Which unlinked segments may be admitted at all, where they enter the tree and how they are numbered, which version of a key survives, how keys collate and what a delete leaves behind are decisions the deployed release makes rather than yours, and every shard boundary and every plan is wrong until the base and the repaired manifest are rebuilt at their expected paths.

Then restore `/app/workflow/rebuild_index.py`, which was carried over from a migration trial and implements a branch this node is not on. Preserve its `--input` and `--output-dir` options and their defaults, and always read the repaired manifest and `/app/data/index_policy.json` from their fixed absolute paths; `--input` selects the base only. `/app/docs/index_contract.json` is the output contract: paths, schemas, required fields, types and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/docs/release_notes`, which carries every release the product has shipped; the manifest records which one this node runs, and notes published after it describe behaviour this node must not have. A run writes exactly `/app/output/summary.json`, `/app/output/shard_index.json` and `/app/output/compaction_plan.jsonl`: standard library only, identical across reruns, correct against a held-out base, inside the contract's 120-second budget, and leaving the frozen snapshot in `/app/workflow` untouched.
